apiVersion: v1
kind: Service
metadata:
  name: subject-wl
spec:
  type: LoadBalancer
  ports:
   - port: 8080
     name: http
     protocol: TCP
     targetPort: 8080
  selector:
      app: subject-wl